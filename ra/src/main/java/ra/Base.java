package ra;

import io.restassured.response.Response;

public class Base {
	public static Response response;
	
	 
	/*
	 * public Base(String endpoint, String uri) { baseURI = endpoint; basePath =
	 * basePath(uri);
	 * 
	 * response = getrequest(); }
	 */

}
